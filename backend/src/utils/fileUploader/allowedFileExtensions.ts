// @ts-nocheck
const images = ['.jpg', '.png', '.jpeg'];
const docs = ['.pdf', '.doc', '.docx'];
const videos = ['.mp4'];

const allowed_ext = {
  images: images,
  docs: docs,
  videos: videos
};

export default allowed_ext;
